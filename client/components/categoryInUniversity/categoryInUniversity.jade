template(name='categoryInUniversity')
    li.list-group-item
        .h4
            =category.name
        ul.list-group
            +each subCategory
                +categoryInUniversity category=this university=../university
            if isLeaf
                +each comments
                    +comment
                if canCreate
                    +addComment
